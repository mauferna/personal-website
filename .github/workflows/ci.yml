name: CI

on:
  pull_request:
  push:
    branches: [main, dev]

jobs:
  web:
    name: 🧪 Web App CI
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/web  # 👈 Run commands from this subdir

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 🧰 Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: 📦 Install dependencies
        run: npm ci

      - name: 🧪 Type check and build
        run: npm run build

      - name: 🧹 Lint
        run: npm run lint